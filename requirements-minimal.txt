# Minimal Working Requirements - Tested on Python 3.11
# Install: pip install -r requirements-minimal.txt

# Core API (no conflicts)
fastapi>=0.100.0,<0.105.0
uvicorn>=0.23.0,<0.25.0
pydantic>=2.0.0,<2.6.0

# HTTP
httpx>=0.24.0
requests>=2.31.0

# ML (smaller versions for compatibility)
torch>=2.8.0,<2.9.0
transformers>=4.30.0,<4.40.0
onnxruntime>=1.15.0,<1.17.0

# Data
pandas>=2.0.0,<2.2.0
datasets>=2.14.0,<3.0.0

# Azure
azure-storage-blob>=12.18.0
azure-data-tables>=12.4.0

# Security (critical for fixes)
cryptography>=46.0.7
keyring>=24.0.0

# Utils
pyyaml>=6.0
pytest>=7.4.0
slowapi>=0.1.9

# UI (optional - install separately if needed)
# gradio>=4.0.0
